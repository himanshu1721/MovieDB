import { StyleSheet } from "react-native";
import { moderateScale } from "../../../themes";

const styles = StyleSheet.create({
  separatorView: { width: moderateScale(20) },
});

export default styles;
